#include "../libs.hpp"

/**1. Grid Map이 주어짐.
 * 2. 가는 길에 체력이 +- 될 수 있고,
 * 3. 최소 거리로 도달하기 위해 가져야할 K의 최소 체력을 구하는 문제
 *    => 입을 수 있는 최소한의 피해량을 구하는 문제.
 * 4. 시작은 Top-Left 도착은 Bottom-Right
 * 
*/
class Solution {
public:
    int calculateMinimumHP(vector<vector<int>>& DG) {
        int m(DG.size()), n(DG[0].size());
        long long acc(0); // Accumulated damages.
        
    }   
};
int main()
{

    Solution S;
    return 0;
}